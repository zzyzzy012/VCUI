# Button 组件

## 基础用法
使用 `type`、`plain`、`circle`、`round` 来定义按钮的样式
<preview path="../demo/Button/Basic.vue" title="基础Button" description="基础Button用法"></preview>

## 不同尺寸
使用 `size` 定义按钮不同大小，属性为 `large` | `samll`
<preview path="../demo/Button/Size.vue" title="不同尺寸按钮" description="不同尺寸按钮用法"></preview>

## 禁用状态
使用 `disabled` 属性定义按钮禁用
<preview path="../demo/Button/Disabled.vue" title="禁用按钮" description="禁用按钮用法"></preview>

## 加载状态
使用 `loading` 属性定义加载状态按钮
<preview path="../demo/Button/Loading.vue" title="加载状态按钮" description="加载状态按钮用法"></preview>

## 使用图标
使用 `icon` 属性定义按钮图标
<preview path="../demo/Button/Icon.vue" title="图标按钮" description="图标按钮用法"></preview>